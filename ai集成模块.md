### ai集成模块



#### 背景

当前项目作为后续业务主题的模板项目，很多业务主题场景通常都需要涉及到ai创新的内容，增加项目亮点，核心是后台管理员能够配置相关的ai模型配置信息，参考给定的scripts下脚本文件，诸如当前scripts存在一个脚本文件为图/文生视频api脚本，选用的是火山引擎的模型api需要基于此背景完善该模块，尽可能详细模块内容，便于后续业务主题直接复用



#### 需要支持内容

1. 后台管理员能够配置ai参数信息，无需通过脚本手动配置
2. 需要支持在后台管理通过相关图片、文字提示词后台生成最终的视频/语音/3d图像等内容信息，采用异步方式，通过celery + redis/rabbitmq实现，不得阻塞
3. 生成时不同状态需要自动回显，判断状态信息，诸如待生成、生成中、失败、成功等，并标注颜色



#### 注意事项

1. 需要适配当前项目，诸如当前项目采用uv统一管理依赖

2. 将最终修改后的内容沉淀为docs文档和agent-docs操作文档